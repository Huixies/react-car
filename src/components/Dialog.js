
import React, {Component} from 'react';

import './Dialog.css';

class Dialog extends Component {
  constructor(...args) {
    super(...args);
  }

  render() {
    return (
      <div>
        
      </div>
    );
  }
}

export default Dialog;
